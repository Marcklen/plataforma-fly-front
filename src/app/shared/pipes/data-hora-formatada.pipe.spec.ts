import { DataHoraFormatadaPipe } from './data-hora-formatada.pipe';

describe('DataHoraFormatadaPipe', () => {
  it('create an instance', () => {
    const pipe = new DataHoraFormatadaPipe();
    expect(pipe).toBeTruthy();
  });
});
